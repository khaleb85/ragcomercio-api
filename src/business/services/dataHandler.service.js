class DataHandlerService {
    convertEquipment(obj) {
        let foo = / ?(\[.+\])? (\[\d+\]$)/g;

        const singleCard = /\[(.+)\*(\d)\]/g;

        const alotCards = /\[(.+)\]/g;
    }
}

export default DataHandlerService;
